# QA Review — Wave 1 (integration-w1) · Minions Team 21 · 2026-09-12

Reviewed on a dedicated worktree from `origin/task/integration-w1`, re-run after the concurrent
Alembic fix. Full suites run twice each; throwaway Postgres per the blessed pattern.

## VERDICT: PASS-with-conditions (one blocker — B1, fixed same day)

## Part 1 — the two red harness lines: FIXED
Both `test_c6_9` clauses now build the app via `tests/ops_harness.py::build_ops_app()` — the
shared form of the C5 suite's `_route_table_app()`, already walked by `test_app_wiring.py`, so the
contract clause and the wiring test see ONE application. No assertion changed. The bearer walk is
built with the machine lane ON so it cannot pass for the wrong reason. Both clauses
mutation-proved (router-level bearer → clause 1 fails; a POST on the read surface → clause 2
fails; nothing else). One clause was found wrong twice over (a list-typed lane check made the
fallback take the bare `create_app()` path regardless) — rewritten.

## Part 2 — author=asserter re-reviews: ALL ACCEPTED, with evidence
- Three completed C6 test-9 bodies (integration fixer): each caught its own single-point mutant
  and only its own. `ops_harness.py` living outside `tests/contracts/` accepted (→ ruling R5
  change control); clause 3's double bearer assertion accepted with the reason corrected
  (sub-case A carries ADR-035's "never 403"; sub-case B is defence-in-depth).
- Stream B's two self-activated C2 bodies: accepted — 3 mutants caught incl. the additive A-2
  usage assertion; `_AlwaysInvalidOutput` verified necessary (the frozen fake's marker succeeds
  on call 2 by design).
- Spine's "no QA assertion changed" claim: VERIFIED by diff — every removed `assert` in the whole
  diff is inside a `pending(...)` string.

## Blocker B1 — mounted C4 surface 500s for an authorised owner
`api/routes/approvals.py:266` — route awaited methods the frozen service protocol does not
declare; any conformant service 500s. The fake was correctly NOT reshaped to match the caller.
**FIXED same day (`cf1fe38`)**: list/get moved to a read model (C6 precedent; one shared query
path so C4 and C6 reads cannot point at two databases), decide stays on the service (a CAS —
a second implementation of a transition is a second way to lose one), effect-hooks made symmetric
and logged; a `decide` callable-shape question (sync signature + caller-supplied `now` vs an HTTP
layer that owns no clock) recorded for the SA in integration-w1 §8.3 — the route answers 501
naming the gap, never a 500 or a guessed clock. New authorised-path suite
(`test_mounted_surface.py`) covers list/filter/cursor/422/detail-no-continuation/404/decide 200→
409/refuse/501, all through the real `create_app` with a real login cookie. Final counts after
fix: **912/0/4 SQLite · 951/0/4 with the Postgres leg**, twice each.

## Cross-lane sweep
Gateway aliases 5/5 · capability→model→provider chains resolve · ports 5433/4000/5680/8000/3001
agree across compose/.env.example/ARCH §5 · all publishes loopback. Gaps found: sweeper + seam
env vars missing from .env.example and ARCH §5 (S1 — both halves landed same day);
`config/agents.yaml` grants test-only `fake_tool` (S2 — ruled R1: structurally inert, grants only
narrow the adapter catalogue; startup warning owed in wave 2); web `pnpm test run` parses `run`
as a filter (N3).

## Independently verified headline claims
Spine 13/13 twelve-stage turn · Stream A ALLOW-no-burn · Stream B parity + 13 non-vacuous
tripwire tests · Stream D CAS race on `postgresql+psycopg` · 403→401 across a 7-route ×
8-credential matrix (zero `Authorization` requests met a 403) · Alembic fix on fresh Postgres
(one head, upgrade/downgrade/upgrade clean) · web build exit 0, 38/38.

Post-review sign-off: the B1 fixer's additive `approvals_factory=` keyword on ops_harness.py
reviewed under R5 change control (see task files).
