# QA Review — Wave 2 (rounds 1+2) · Minions Team 21 · 2026-09-12

Round 1: PASS-with-conditions — the DM's R7.1 transcription verified with red-green proof; the
fake-wired decision test added, closing the recorded 501 coverage gap on both historical
configurations; all five wiring headline claims reproduced (incl. the autogenerate fence
emitting literally `pass` against a real database); counts 947/992. Conditions closed in-wave.

Round 2 (@265fae8): **PASS-with-conditions.**

- **Counts exact**: 1085/47 twice (SQLite), 1177/4 (Postgres); the -7 delta re-derived
  arithmetically from the replaced mount module (12 → 5), no other module moved.
- **Cross-lane value sweep**: 9 value groups agree across .env.example / settings / ARCH §5 /
  compose / workflow exports / tests (the n8n /mcp/sunil path at every executable site; the
  Stream-C settings trio; the openhands URL + per-field named host with a non-widening pin).
  Two ADR prose stragglers = F-3 (LOW).
- **Stream headlines re-proven**: C — recall verified at prompt depth on real Postgres, with the
  negative-leak and dead-provider degrade probes, and stage-3 evidence on the durable audit row;
  E — the recorded 2.38.5 handshake replayed through the production adapter path, and the
  bearerAuth export pin mutation-verified red/green; F — the parked merge on the real chokepoint:
  typed ApprovalRef, PENDING C4 row, push-ran-merge-did-not.
- **Dormancy**: R16's mutation reproduces exactly, including the production loader refusing the
  half-wired state with the verbatim ToolsConfigError.
- **Frozen suites hash-identical to origin/V2** (tree hashes compared); contract marker 299/0.
- **author=asserter sweep: EMPTY** — no lane wrote assertions about its own work in the frozen
  suites this wave. Open-items ledger: 5/5 spot-checks have owners and owning rounds.

**Findings:** F-1 MED — R16's replacement module dropped the catalogue-wide fix_and_pr scan with
no successor and no note (SA + applier jointly); re-land as a standalone module in w2r3.
F-2 MED, pre-existing on V2 — the PRODUCTION park exit's ApprovalRef (manager.py:527) is covered
by nothing: approval_ref=None survives both legs green; named w2r3 item. F-3/F-4/F-5 LOW/NIT.
All ledgered with owners in docs/tasks/w2r3-conditions.md.
