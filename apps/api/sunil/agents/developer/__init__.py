"""Stream F — the developer/QA execution seam (ADR-030 §4).

``client.py`` is SUNIL's own narrow seam; ``openhands_http.py`` is the one
module that knows OpenHands' HTTP shape; ``agent.py`` is the governed agent.
"""
