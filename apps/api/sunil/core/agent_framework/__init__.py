"""The agent framework (`ARCHITECTURE_V1.md` §10.1) — `AgentContext`, the
`Agent` protocol, and the runner that mints `ExecutionMetadata` an agent
cannot mint itself (ADR-004 Amendment 1, guard site 2).
"""
