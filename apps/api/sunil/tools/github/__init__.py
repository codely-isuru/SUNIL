"""The GitHub tool adapter (T8) — M1's one tool.

`adapter.GitHubAdapter` implements `sunil.core.tool_framework.base.ToolAdapter`;
`projection.py` is the security component that shapes everything the
adapter returns before it can reach a model.
"""

from __future__ import annotations
